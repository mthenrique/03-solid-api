{
  "extends": "@commitlint/config-conventional",
  "rules": {
    "subject-case": [2, "always", "lower-case"],
    "subject-max-length": [2, "always", 72],
    "type-enum": [2, "always", ["feat", "fix", "docs", "style", "refactor", "test", "chore", "revert", "build"]]
  }
}